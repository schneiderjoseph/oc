# Livres & guides Word

Placer ici les documents `.docx` de référence :

| Fichier | Emplacement |
|---------|-------------|
| `Comprendre_Optimum_Control_V2.docx` | À copier ici |
| `Exercice_Pratique_Optimum_Control.docx` | `exercice_comptoir/` (déjà versionné) |
| `Exercice_Parcours_Lineaire.docx` | `exercice_comptoir/` (déjà versionné) |
| `Exercice_Corrige_Detaille.docx` | `exercice_comptoir/` (déjà versionné) |

> Les `.docx` ne sont pas sur ce disque actuellement — les copier depuis la machine de travail principale avant commit.

Les **scripts de génération** sont dans `scripts/build/` (`build_exercice.py`, `rebuild_final.py`, etc.).

# Livres & guides Word

Placer ici les documents `.docx` de référence :

| Fichier | Description |
|---------|-------------|
| `Comprendre_Optimum_Control_V2.docx` | Guide de compréhension OC (triangle Items/Preps/Products) |
| `Exercice_Pratique_Optimum_Control.docx` | Exercice Comptoir du Marché |
| `Exercice_Parcours_Lineaire.docx` | Parcours linéaire formation v5 |
| `Contrat_Prestation_OC_LaReserve_*.docx` | Contrats mission (si besoin archivage) |

> Les `.docx` ne sont pas sur ce disque actuellement — les copier depuis la machine de travail principale avant commit.

Les **scripts de génération** sont dans `scripts/build/` (`build_exercice.py`, `rebuild_final.py`, etc.).
